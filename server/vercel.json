{
  "version": 2,
  "buildCommand": "mkdir -p .vercel/output/static && echo 'done'",
  "installCommand": "echo 'no install'",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "api/index.js" }
  ],
  "ignore": [
    "api/index.ts",
    "node_modules",
    "pnpm-lock.yaml",
    "package-lock.json"
  ]
}